# No.017 · 机械禽龙

场景：银杏林地 / GINKGO WOODLAND。

拇指尖刺隐在机械前肢之间，喙部轻触低垂叶片。机械禽龙穿行银杏林地。

卡片定义见 `card-config.json`，内置 image_gen 提示词见 `PROMPTS.json`。制作素材位于本机 `assets/`，可编辑工程为 `card.blend`，三视图在 `renders/`；网站副本为 `web/cards/017/`。
