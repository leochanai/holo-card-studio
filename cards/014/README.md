# No.014 · 机械腕龙

场景：云雾杉林 / MISTY CONIFERS。

修长前肢托起高耸肩背，长颈伸向云雾间的树冠。机械腕龙行走在杉林深处。

卡片定义见 `card-config.json`，内置 image_gen 提示词见 `PROMPTS.json`。制作素材位于本机 `assets/`，可编辑工程为 `card.blend`，三视图在 `renders/`；网站副本为 `web/cards/014/`。
