# No.012 · 机械剑龙

场景：晚霞蕨原 / SUNSET FERNLAND。

成列背板映出铜色晚霞，尾端尖刺闪过冷光。机械剑龙穿行开阔蕨原。

卡片定义见 `card-config.json`，内置 image_gen 提示词见 `PROMPTS.json`。制作素材位于本机 `assets/`，可编辑工程为 `card.blend`，三视图在 `renders/`；网站副本为 `web/cards/012/`。
