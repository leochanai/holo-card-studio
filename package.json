{
  "name": "neon-genesis-holo-cards",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "npm ci --prefix web --no-audit --no-fund",
    "build": "node scripts/build-site.mjs",
    "start": "node web/server.mjs"
  }
}
